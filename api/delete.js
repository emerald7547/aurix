import postgres from 'postgres';
let sql;function db(){if(!process.env.POSTGRES_URL)throw new Error('POSTGRES_URL is not configured');if(!sql)sql=postgres(process.env.POSTGRES_URL,{ssl:'require',max:1,prepare:false});return sql}
export default async function handler(req,res){if(req.method!=='DELETE')return res.status(405).json({error:'Method not allowed'});try{const id=String(req.query.id||'');if(!id)return res.status(400).json({error:'ID required'});await db()`DELETE FROM aurix_submissions WHERE id=${id}`;return res.status(200).json({success:true})}catch(e){console.error(e);return res.status(500).json({error:'Failed to delete submission'})}}