import { ensureTable,getDb,getUserFromRequest } from '../lib/db.js';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const user=await getUserFromRequest(req);if(!user)return res.status(401).json({error:'Please log in'});const db=getDb();const rows=await db`SELECT id,business_name,status,status_message,submitted_at,approved_at,completed_at FROM aurix_submissions WHERE user_id=${user.id} OR LOWER(email)=LOWER(${user.email}) ORDER BY submitted_at DESC`;return res.status(200).json({email:user.email,submissions:rows})}catch(e){console.error(e);return res.status(500).json({error:'Could not load status'})}}
