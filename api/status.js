import { ensureTable, getDb } from '../lib/db.js';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{await ensureTable();const id=req.query?.id;if(!id)return res.status(400).json({error:'Missing id'});const db=getDb();const r=await db`SELECT id,business_name,status,status_message,submitted_at,approved_at,completed_at FROM aurix_submissions WHERE id=${id} LIMIT 1`;if(!r.length)return res.status(404).json({error:'Submission not found'});return res.status(200).json({id:r[0].id,businessName:r[0].business_name,status:r[0].status,statusMessage:r[0].status_message||'',submittedAt:r[0].submitted_at,approvedAt:r[0].approved_at,completedAt:r[0].completed_at});}catch(e){console.error(e);return res.status(500).json({error:'Failed to load status'});}}
