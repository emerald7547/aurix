import postgres from 'postgres';
let sql;
function getSql(){if(!process.env.POSTGRES_URL)throw new Error('POSTGRES_URL is not configured');if(!sql)sql=postgres(process.env.POSTGRES_URL,{ssl:'require',max:1,prepare:false});return sql}
export async function ensureTable(){const db=getSql();await db`CREATE TABLE IF NOT EXISTS aurix_users (id TEXT PRIMARY KEY,email TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`;await db`CREATE TABLE IF NOT EXISTS aurix_sessions (token TEXT PRIMARY KEY,user_id TEXT NOT NULL REFERENCES aurix_users(id) ON DELETE CASCADE,expires_at TIMESTAMPTZ NOT NULL)`;await db`CREATE TABLE IF NOT EXISTS aurix_submissions (id TEXT PRIMARY KEY,name TEXT NOT NULL,email TEXT NOT NULL,phone TEXT,instagram TEXT,business_name TEXT NOT NULL,business_description TEXT,goal TEXT,style TEXT,colors TEXT,has_content TEXT,additional_info TEXT,timeline TEXT,features JSONB NOT NULL DEFAULT '[]'::jsonb,images JSONB NOT NULL DEFAULT '[]'::jsonb,status TEXT NOT NULL DEFAULT 'pending',status_message TEXT,approved_at TIMESTAMPTZ,completed_at TIMESTAMPTZ,submitted_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),user_id TEXT)`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS user_id TEXT`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS instagram TEXT`;await db`ALTER TABLE aurix_submissions ALTER COLUMN phone DROP NOT NULL`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS images JSONB NOT NULL DEFAULT '[]'::jsonb`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'pending'`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS status_message TEXT`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS approved_at TIMESTAMPTZ`;await db`ALTER TABLE aurix_submissions ADD COLUMN IF NOT EXISTS completed_at TIMESTAMPTZ`}
export function getDb(){return getSql()}
export async function getUserFromRequest(req){await ensureTable();const token=(req.headers.cookie||'').match(/(?:^|; )aurix_session=([^;]+)/)?.[1];if(!token)return null;const db=getSql();const rows=await db`SELECT u.id,u.email FROM aurix_sessions s JOIN aurix_users u ON u.id=s.user_id WHERE s.token=${token} AND s.expires_at>NOW()`;return rows[0]||null}
export function setSessionCookie(res,token){res.setHeader('Set-Cookie',`aurix_session=${token}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=604800`)}
